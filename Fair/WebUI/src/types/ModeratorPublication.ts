export type ModeratorPublication = {
  id: string
  flags: string
  categoryId: string
  categoryTitle: string
  creatorId: string
  productId: string
  productUpdated: number
  authorId: string
  authorTitle: string
}
